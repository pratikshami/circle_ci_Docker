
  
machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t circleci/circleci-job .

test:
  override:
    - docker run -d -p 3010:3010 circleci/circleci-job; sleep 10
    

deployment:
  hub:
    branch: master
    commands:
      - docker login -e $pratiksharmisal@gmail.com -u $pratiksha12 -p $rahul@110
      - docker push circleci/circleci-job
