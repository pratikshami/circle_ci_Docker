
  
machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t circle_ci_Docker/circleci-job .

test:
  override:
    - docker run -d -p 3010:3010 circle_ci_Docker/circleci-job; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://localhost:9200

deployment:
  hub:
    branch: master
    commands:
      - docker login -e $pratikshami@cybage.com -u $pratiksha12 -p $rahul@110
      - docker push circle_ci_Docker/circleci-job